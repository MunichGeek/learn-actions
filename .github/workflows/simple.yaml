name: Super Cool Workflow
on: push 
jobs:
  cool_first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Run a simple command
        run: |
          import random
          for _ in range(5):
            print(f"A random integer is {random.randint()}")
        shell: python